\section{Marco teórico}

\subsection{El teorema de Thévenin}

Dada la relación lineal entre corriente y voltaje expresada por la \texttt{Ley de Ohm} ($V=IR$) y la conservación de la carga y energía expresada por las \texttt{Leyes de Kirchhoff}, podemos analizar redes eléctricas complejas \cite{kuphaldt_lessons_dc}.

\vspace{0.5cm}

\textbf{Ley de Corrientes de Kirchhoff (Nodos):}
\begin{equation}
    \sum_{i=1}^{n} I_i = 0
    \label{eq:lck}
\end{equation}

\textbf{Ley de Voltajes de Kirchhoff (Mallas):}
\begin{equation}
    \sum_{i=1}^{n} V_i = 0
    \label{eq:lvk}
\end{equation}

\vspace{0.5cm}

Imaginamos que encerramos un complejo sistema de ecuaciones lineales (el circuito) en una caja negra, dejando únicamente dos terminales expuestas ($A$ y $B$). Para analizar su respuesta, conectamos una fuente de corriente de prueba $I_{\text{test}}$ en estas terminales extrayendo corriente, y buscamos determinar cuál será la tensión $V_{AB}$.

Aplicando el \textbf{principio de superposición} (válido debido a la linealidad del sistema), el voltaje total en las terminales $V_{AB}$ es la suma de dos efectos independientes actuando por separado:
\begin{enumerate}
    \item El voltaje producido por las fuentes de energía internas del circuito ($V_{\text{th}}$).
    \item La caída de voltaje producida por la corriente externa $I_{\text{test}}$ a través de la resistencia equivalente vista desde las terminales ($I_{\text{test}} \cdot R_{\text{th}}$).
\end{enumerate}

De este modo, el \textbf{Teorema de Thévenin} establece que cualquier circuito lineal de corriente directa (\textbf{CD}) que contenga resistencias y fuentes de energía (independientes o dependientes) puede reemplazarse por un circuito equivalente sencillo visto desde sus terminales ($A$ y $B$).

\begin{figure}[htbp]
    \centering
    \begin{circuitikz}
        \draw (0,0) -- (4,0)
            node [ocirc, label=below:B] (B) {};
        \draw (0,0) to [V, l=$V_{\mathrm{th}}$] (0,3)
            to [R, l=$R_{\mathrm{th}}$] (4,3)
            node [ocirc, label=above:A] (A) {};
    \end{circuitikz}
    \caption{Circuito equivalente de Thévenin.}
    \label{fig:thevenin}
\end{figure}

Este circuito equivalente consta de solo dos componentes conectados en serie:
\begin{itemize}
    \item \textbf{Voltaje de Thévenin ($V_{\mathrm{th}}$):} Es el voltaje de circuito abierto medido entre los terminales $A$ y $B$ (cuando no hay ninguna carga conectada). Se calcula utilizando métodos de análisis como la \texttt{Ley de Corrientes de Kirchhoff (LCK)} o la \texttt{Ley de Voltajes de Kirchhoff (LVK)}.
    \item \textbf{Resistencia de Thévenin ($R_{\mathrm{th}}$):} Es la resistencia equivalente vista desde los terminales $A$ y $B$ cuando todas las fuentes independientes de energía han sido desactivadas (fuentes de voltaje cortocircuitadas y fuentes de corriente abiertas).
\end{itemize}

Para encontrar el circuito equivalente de \texttt{Thévenin} respecto a una "carga" (una resistencia o componente de interés), sigue este algoritmo:

\begin{algorithm}
\caption{Algoritmo para encontrar circuito equivalente de \texttt{Thévenin}}
\label{alg:Thévenin}

\begin{algorithmic}[1] % El [1] numera cada línea

\Procedure{EncontrarCircuitoThévenin}{}
    \Statex \textbf{Entrada:} circuito con una carga conectada entre $a$ y $b$
    \Statex \textbf{Salida:} $V_{th}$, $R_{th}$ y circuito equivalente
    \State \textbf{Aislar:} retirar la carga para dejar abiertos los terminales $a$ y $b$.
    \State \textbf{Hallar $V_{th}$:} analizar el circuito sin carga y calcular el voltaje entre $a$ y $b$.
    \State \textbf{Hallar $R_{th}$:} apagar todas las fuentes independientes.
    \Statex \hspace{\algorithmicindent}\textcolor{algorithmgray}{\scriptsize\textbullet\;} Calcular la resistencia equivalente vista desde $a$ y $b$.
    \Statex \hspace{\algorithmicindent}\textcolor{algorithmgray}{\scriptsize\textbullet\;} Reducir el circuito hacia adelante, como en el ejemplo de la Figura~\ref{fig:reduccion_thevenin_completa}.
    \State \textbf{Construir:} conectar $V_{th}$ en serie con $R_{th}$.
\EndProcedure

\end{algorithmic}
\end{algorithm}

\begin{figure}[htbp]
    \centering
    
    % --- SUBFIGURA A: RED RESISTIVA ORIGINAL CON FUENTE ---
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \begin{circuitikz}[scale=0.52, transform shape]
            % Fuente de voltaje usando simbología americana pura
            \draw (-2,4) to[american voltage source, l=$V_s$] (-2,0)
                  to[short] (0,0);
            \draw (-2,4) to[short] (0,4);

            % Barra superior (Terminal a)
            \draw (0,4) node[ocirc, label=above:$a$] {}
                  to[R, l=$R_1$, *-*] (4,4)
                  to[R, l=$R_2$, *-*] (8,4)
                  to[R, l=$R_3$, *-*] (12,4);
                  
            % Barra inferior (Terminal b)
            \draw (0,0) node[ocirc, label=below:$b$] {}
                  to[R, l=$R_4$, *-*] (4,0)
                  to[short, *-*] (8,0)
                  to[short, -*] (12,0);

            % Ramas verticales y diagonales
            \draw (4,4) to[R, l=$R_5$] (4,0);
            \draw (8,4) to[R, l=$R_6$] (8,0);
            \draw (12,4) to[short] (12,0);

            \draw (8,4) to[R, l=$R_7$] (4,0);
            \draw (8,4) to[R, l=$R_8$] (12,0);
        \end{circuitikz}
        \caption{Red resistiva original con fuente $V_s$.}
        \label{fig:circuito_original}
    \end{subfigure}
    \hfill
    % --- SUBFIGURA B: PRIMERA REDUCCIÓN (LADO DERECHO) ---
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \begin{circuitikz}[scale=0.52, transform shape]
            \draw (-2,4) to[american voltage source, l=$V_s$] (-2,0) -- (0,0);
            \draw (-2,4) -- (0,4);

            \draw (0,4) node[ocirc, label=above:$a$] {}
                  to[R, l=$R_1$, *-*] (4,4)
                  to[R, l=$R_2$, *-*] (8,4);
                  
            \draw (0,0) node[ocirc, label=below:$b$] {}
                  to[R, l=$R_4$, *-*] (4,0)
                  to[short, *-*] (8,0);

            \draw (4,4) to[R, l=$R_5$] (4,0);
            \draw (8,4) to[R, l=$R_7$] (4,0);
            \draw (8,4) to[R, l=$R_{\mathrm{eq1}}$] (8,0);
        \end{circuitikz}
        \caption{Paso 1: Simplificación del bloque derecho ($R_{\mathrm{eq1}}$).}
        \label{fig:circuito_paso1}
    \end{subfigure}

    \vspace{0.4cm}

    % --- SUBFIGURA C: SEGUNDA REDUCCIÓN (BLOQUE CENTRAL) ---
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \begin{circuitikz}[scale=0.55, transform shape]
            \draw (-2,4) to[american voltage source, l=$V_s$] (-2,0) -- (0,0);
            \draw (-2,4) -- (0,4);

            \draw (0,4) node[ocirc, label=above:$a$] {}
                  to[R, l=$R_1$, *-*] (4,4);
                  
            \draw (0,0) node[ocirc, label=below:$b$] {}
                  to[R, l=$R_4$, *-*] (4,0);

            \draw (4,4) to[R, l=$R_{\mathrm{eq2}}$] (4,0);
        \end{circuitikz}
        \caption{Paso 2: Simplificación del bloque central ($R_{\mathrm{eq2}}$).}
        \label{fig:circuito_paso2}
    \end{subfigure}
    \hfill
    % --- SUBFIGURA D: CIRCUITO EQUIVALENTE DE THÉVENIN FINAL ---
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \begin{circuitikz}[scale=0.60, transform shape]
            \draw (0,0) to[american voltage source, l=$V_{\mathrm{th}}$] (0,3)
                  to[R, l=$R_{\mathrm{th}}$] (4,3)
                  to[short] (5,3)
                  node[ocirc, label=above:$a$] {};
            \draw (0,0) -- (5,0)
                  node[ocirc, label=below:$b$] {};
        \end{circuitikz}
        \caption{Circuito equivalente de Thévenin final.}
        \label{fig:circuito_thevenin}
    \end{subfigure}

    \caption{Proceso de reducción progresiva 1 a 1 hasta obtener el equivalente de Thévenin.}
    \label{fig:reduccion_thevenin_completa}
\end{figure}




